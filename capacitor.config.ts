import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicframework.lostandfound',
  appName: 'Lost_And_Found',
  webDir: 'dist'
};

export default config;
